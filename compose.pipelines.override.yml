version: "3.9"
services:
  data_pipelines:
    build:
      context: .
      dockerfile: docker/pipelines/Dockerfile

  startup_container:
    build:
      context: .
      dockerfile: docker/pipelines/Dockerfile
