version: "3.9"
services:
  data_pipelines:
    build:
      context: .
      dockerfile: docker/data_pipelines/Dockerfile

  startup_container:
    build:
      context: .
      dockerfile: docker/data_pipelines/Dockerfile
