version: "3.9"
services:
  rag_backend:
    build:
      context: .
      dockerfile: docker/backend/Dockerfile

  startup_container:
    build:
      context: .
      dockerfile: docker/backend/Dockerfile
